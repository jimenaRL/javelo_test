from setuptools import setup

setup(name='perfManSer',
      version='1.0',
      description='perfManSer library for Javelo technical test',
      author='Jimena Rooy-Letelier',
      author_email='jimena@melix.net',
      install_requires=[],
      packages=['perfManSer'],
      zip_safe=False)
